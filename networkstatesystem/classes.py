
class device(object):
	"""Definitaion of a device"""
	def __init__(self, hostname, ipaddress, type, routingtable, interfacetable):
		super(device, self).__init__()
		self.hostname = hostname
		# os version
		# ip address
		# type
		# interface info
		# routing info
		